import React from 'react';
import { Text } from 'react-native';
import { CollectionScreenStyle } from './CollectionScreen.style';

const CollectionScreen = () => {
    return (
        <CollectionScreenStyle>
            <Text>
                CollectionPage
            </Text>
        </CollectionScreenStyle>
    )
};

export default CollectionScreen;